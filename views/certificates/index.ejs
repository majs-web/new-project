<!DOCTYPE html>
<html lang="en">
    <%- include('../includes/head', {title: 'Register Certificate | Bondeappen'}) %>
<body>
    <%- include('../includes/header') %>

    <div class="general-setup">
    <h2>My certificates</h2>
    <ul class="news-list">
        <% certificates.forEach(certificate => { %>
        <li>
            <% if(certificate) { %> 
            <h4><%= certificate.name %>: <%= certificate.content %></h4>
            <a class="signin-button" href="/certificates/<%= certificate.slug %>">Read more</a>
            <% } else { %>
            <%= certificate.name %> [NOT FOUND] 
            <% } %>
        </li>
        <% }) %>
    </ul>
    <a class="cta-button" href="/certificates/new">Register new certficate</a><br/>
    <a class="signin-button" href="/profile">Back</a>
    </div>

    <%- include('../includes/footer') %>
</body>
</html>